import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'onesignal.v807',
  appName: 'onesignal-807',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
